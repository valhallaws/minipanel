# Only the dedicated control-plane identity may invoke the agent.
# Permit the fixed agent only; never permit bash, php, or arbitrary commands.
minipanel-control ALL=(root) NOPASSWD: /usr/local/bin/minipanel-agent *
