<div class="control-page {{ $embedded ? 'embedded-domain' : '' }}" x-data="{ tab: 'overview', drawer: '', hostingTab: 'hosting' }" wire:init="initializeSiteSummary" @if($activity->whereIn('status', ['queued','running'])->isNotEmpty() || ($snapshot['status'] ?? '') === 'queued') wire:poll.1500ms @endif>
@unless($embedded)<header class="topbar"><a class="brand" href="/"><img class="freyja-brand-mark" src="/brand/freyja-isotipo.png" alt="">Freyja</a><div class="server">{{ $site->domain }}</div><a class="ghost" href="/">← Sitios</a></header>
<div class="control-heading"><div><p class="eyebrow">ADMINISTRACIÓN DEL SITIO</p><h1>{{ $site->name }}</h1></div><span class="badge">{{ $site->status }}</span><a class="secondary" href="{{ route('sites.files', $site) }}">Archivos</a></div>
@endunless
@if(session('notice'))<div class="notice">{{ session('notice') }}</div>@endif
<div class="control-layout">
<aside class="control-summary"><div class="domain-emblem">@if($snapshotExists)<img class="site-snapshot" src="{{ route('sites.snapshot', ['site' => $site, 'v' => $snapshot['version'] ?? 0]) }}" alt="Vista previa de {{ $site->domain }}">@else<small>{{ ($snapshot['status'] ?? '') === 'queued' ? 'Capturando sitio…' : 'Vista previa no disponible' }}</small>@endif</div><button wire:click="refreshSnapshot(true)" wire:loading.attr="disabled" class="ghost">Actualizar captura</button>@if(($snapshot['status'] ?? '') === 'failed')<small>No se pudo capturar. Comprueba que el sitio responda; se conserva la imagen anterior.</small>@endif<h2>{{ $site->domain }}</h2><p>{{ strtoupper($site->runtime) }}</p><section class="site-statistics"><header><strong>Estadísticas</strong><button type="button" class="ghost" wire:click="loadSiteStatistics(true)" wire:loading.attr="disabled" title="Actualizar estadísticas">↻</button></header><dl><dt>Espacio usado</dt><dd>{{ $siteStatistics['disk'] ?? '—' }}</dd><dt>Tráfico este mes</dt><dd>{{ ($siteStatistics['traffic_available'] ?? false) ? ($siteStatistics['traffic'] ?? '—') : 'Aún sin datos' }}</dd></dl></section><button wire:click="inspect" class="secondary">Inspeccionar proyecto</button><a href="{{ route('sites.files', $site) }}" class="ghost">Explorador de archivos →</a></aside>
<main class="control-main"><nav class="project-tabs" aria-label="Herramientas del sitio">
@foreach(['overview'=>'Resumen','activity'=>'Actividad'] as $key=>$label)
<button @click="tab = '{{ $key }}'" :class="{ 'active': tab === '{{ $key }}' }">{{ $label }}</button>
@endforeach
@if($site->runtime === 'laravel' || $site->has_laravel_repository)<button type="button" @click="tab = 'laravel'; $wire.openLaravel()" :class="{ active: tab === 'laravel' }">Laravel</button>@endif
<button type="button" @click="tab = 'databases'; $wire.openDatabases()" :class="{ active: tab === 'databases' }">Bases de datos</button>
</nav><div class="control-content">
<section x-show="tab === 'laravel'" x-cloak>
@if($laravelOpened && ($site->runtime === 'laravel' || $site->has_laravel_repository))
<livewire:laravel-manager :site="$site" :embedded="true" :key="'laravel-site-'.$site->id" />
@else<p class="muted">Abriendo herramientas Laravel…</p>@endif
</section>
<section x-show="tab === 'databases'" x-cloak>
@if($databasesOpened)
<livewire:database-manager :site="$site" :embedded="true" :key="'databases-site-'.$site->id" />
@else<p class="muted">Abriendo bases de datos…</p>@endif
</section>
<section x-show="tab === 'overview'" x-cloak>
@include('livewire.domain-tools')
</section><section x-show="tab === 'hosting'" x-cloak><h2>Hosting y seguridad</h2>
@include('livewire.hosting-settings')<div class="tool-grid"><button class="tool-tile" @click="drawer = 'nginx'"><span>Nginx</span><small>Abrir herramienta →</small></button><button class="tool-tile" @click="drawer = 'domains'"><span>Dominios y rollback</span><small>Abrir herramienta →</small></button><button class="tool-tile" @click="drawer = 'health'"><span>DNS y salud</span><small>Abrir herramienta →</small></button><button class="tool-tile" @click="drawer = 'danger'"><span>Zona de peligro</span><small>Abrir herramienta →</small></button><button class="tool-tile" @click="tab = 'files'; $wire.openFiles()"><span>Archivos</span><small>Abrir explorador →</small></button></div></section><section x-show="tab === 'artisan'" x-cloak><article class="panel"><h2>Laravel / Artisan</h2><p class="muted">Comandos detectados del proyecto. Cada argumento va en una línea y se audita.</p>
            @if($site->artisan_commands)<form wire:submit="runArtisan" class="stack"><select wire:model="artisanCommand"><option value="">Selecciona un comando</option>@foreach($site->artisan_commands as $command)<option value="{{ $command['name'] }}">{{ $command['name'] }} — {{ $command['description'] }}</option>@endforeach</select><textarea wire:model="artisanArguments" rows="3" placeholder="Argumentos opcionales, uno por línea"></textarea><button class="secondary">Ejecutar Artisan</button></form>@else <p class="muted">Inspecciona el proyecto para descubrir comandos.</p>@endif
        </article><h3>Resultado</h3><div class="command-result">@forelse($activity->where('action', 'artisan') as $run)<div><strong>{{ $run->status }} · {{ $run->created_at->diffForHumans() }}</strong><pre>{{ $run->output }}</pre></div>@empty<p class="muted">Ejecuta un comando para ver su resultado aquí.</p>@endforelse</div></section><section x-show="tab === 'composer'" x-cloak><article class="panel"><h2>Composer</h2><form wire:submit="runComposerCommand" class="stack"><label>composer<input wire:model="composerCommand" placeholder="install --no-dev"></label><button class="primary" wire:loading.attr="disabled">Ejecutar Composer</button></form></article><h3>Resultado</h3><div class="command-result">@forelse($activity->where('action', 'composer') as $run)<div><strong>{{ $run->status }} · {{ $run->created_at->diffForHumans() }}</strong><pre>{{ $run->output }}</pre></div>@empty<p class="muted">Ejecuta un comando para ver su resultado aquí.</p>@endforelse</div></section><section x-show="tab === 'npm'" x-cloak><article class="panel"><h2>Node.js / npm</h2><form wire:submit="runNpmCommand" class="stack"><label>npm<input wire:model="npmCommand" placeholder="run build"></label><button class="primary" wire:loading.attr="disabled">Ejecutar npm</button></form><div class="button-row">@foreach($site->npm_scripts ?? [] as $name => $script)<button wire:click="npm('run', @js($name))" class="secondary">{{ $name }}</button>@endforeach</div></article><h3>Resultado</h3><div class="command-result">@forelse($activity->where('action', 'npm') as $run)<div><strong>{{ $run->status }} · {{ $run->created_at->diffForHumans() }}</strong><pre>{{ $run->output }}</pre></div>@empty<p class="muted">Ejecuta un comando para ver su resultado aquí.</p>@endforelse</div></section><section x-show="tab === 'queues'" x-cloak><article class="panel"><h2>Colas y workers</h2><p class="muted">Crea un servicio systemd por worker y cola.</p><form wire:submit="createQueue" class="form-grid"><label>Queue<input wire:model="queueName" placeholder="emails"></label><label>Workers<input wire:model="queueWorkers" type="number" min="1" max="12"></label><label>Tries<input wire:model="queueTries" type="number" min="1" max="20"></label><label>Timeout<input wire:model="queueTimeout" type="number" min="10" max="3600"></label><div class="form-actions"><button class="secondary">Guardar cola</button></div></form>@foreach($queues as $queue)<p><code>{{ $queue->name }}</code> · {{ $queue->workers }} worker(s) · {{ $queue->tries }} tries · {{ $queue->timeout }}s · <span class="ok">{{ $queue->status }}</span> <button wire:click="removeQueue({{ $queue->id }})" class="ghost">Eliminar</button></p>@endforeach</article></section><section x-show="tab === 'schedule'" x-cloak><article class="panel"><h2>Tareas programadas</h2><p>Programador: {{ $site->scheduler_enabled ? 'Habilitado' : 'Deshabilitado' }}</p><p class="muted">Estado de la última inspección: {{ $site->scheduler_status ?? 'Pendiente' }}</p><button wire:click="inspect" class="secondary">Actualizar inspección</button><p class="muted">Puedes consultar schedule:list desde Artisan para ver las tareas definidas en el proyecto.</p></article></section><section x-show="tab === 'deployment'" x-cloak><h2>Despliegue</h2><dl class="deployment-info"><dt>Repositorio</dt><dd>{{ $site->repository ?: 'Sin repositorio' }}</dd><dt>Rama</dt><dd>{{ $site->branch }}</dd><dt>Commit</dt><dd>{{ $site->current_commit ?: 'Sin inspeccionar' }}</dd></dl><div class="tool-grid"><button class="tool-tile" @click="drawer='script'">Editar script de deploy</button><button class="tool-tile" @click="drawer='webhook'">Webhook</button><button class="tool-tile" @click="drawer='git'">Llaves SSH</button></div><p class="muted">El despliegue se inicia desde la lista de sitios.</p></section>
<section x-show="tab === 'activity'" x-cloak><section class="panel activity"><h2>Actividad</h2>@forelse($activity as $item)<div class="activity-row"><span class="dot {{ $item->status }}"></span><div><strong>{{ $item->action }}</strong><p>{{ $item->output }}</p></div><time>{{ $item->created_at->diffForHumans() }}</time></div>@empty <p class="muted">Sin operaciones todavía.</p>@endforelse</section></section>
</div></main></div>
<footer class="site-summary-footer"><span>Web: <a href="{{ $site->ssl_enabled ? 'https' : 'http' }}://{{ $site->domain }}" target="_blank" rel="noopener">/{{ $site->document_root ?? '.' }} ↗</a></span><span>IP: {{ $serverIp ?: 'Sin configurar' }}</span><span>Root: <code>{{ $site->path }}</code></span><span>PHP {{ $site->php_version }} · SSL {{ $site->ssl_enabled ? 'Activo' : 'Pendiente' }}</span><span>Rama {{ $primaryRepository?->branch ?? $site->branch }}</span><span>Commit <code>{{ $currentCommit ? substr($currentCommit, 0, 8) : '—' }}</code></span><span>Último push {{ $primaryRepository?->last_push_at?->timezone($serverTimezone)->format('d/m/Y H:i') ?? 'Sin push recibido' }}</span></footer>
<div class="settings-backdrop" x-show="drawer !== ''" x-cloak @keydown.escape.window="drawer = ''">
<section class="settings-drawer" :class="{ 'hosting-drawer': drawer === 'hosting' }" role="dialog" aria-modal="true" aria-label="Configuración del sitio"><header><strong>{{ $site->domain }}</strong><button class="ghost" @click="drawer = ''">Cerrar ✕</button></header><div class="settings-body">
<div x-show="drawer === 'hosting'" x-cloak>
    <nav class="project-tabs settings-tabs" aria-label="Configuración de hosting">
        <button type="button" @click="hostingTab = 'hosting'" :class="{ active: hostingTab === 'hosting' }">Hosting</button>
        <button type="button" @click="hostingTab = 'nginx'" :class="{ active: hostingTab === 'nginx' }">Nginx</button>
    </nav>
    <div x-show="hostingTab === 'hosting'" x-cloak>@include('livewire.hosting-settings')</div>
    <article class="panel nginx-settings" x-show="hostingTab === 'nginx'" x-cloak>
        <div class="section-title"><div><h2>Nginx del dominio</h2><p>Estas directivas se insertan dentro del bloque <code>server</code>. Se validan y se revierten automáticamente si Nginx no las acepta.</p></div><button wire:click="saveNginxConfig" class="primary">Aplicar Nginx</button></div>
        <p class="muted">Ejemplos: <code>client_max_body_size 2g;</code>, <code>fastcgi_read_timeout 300s;</code>, <code>proxy_read_timeout 300s;</code> o un bloque <code>location</code> específico.</p>
        <textarea wire:model="nginxConfig" class="env-editor" rows="16" spellcheck="false" placeholder="client_max_body_size 128m;&#10;fastcgi_read_timeout 120s;&#10;location /health { return 200; }"></textarea>
        @error('nginxConfig')<p class="error">{{ $message }}</p>@enderror
    </article>
</div>
<div x-show="drawer === 'git'"><article class="panel"><h2>Llave de despliegue SSH</h2><p class="muted">La llave privada se conserva sólo en el VPS. Copia esta llave pública como Deploy Key en tu proveedor Git.</p>@if($site->deploy_key_public)<textarea class="env-editor" rows="3" readonly>{{ $site->deploy_key_public }}</textarea><div class="button-row"><button wire:click="createDeployKey(true)" class="secondary">Rotar llave</button><button wire:click="removeDeployKey" class="ghost">Revocar del VPS</button></div>@else<button wire:click="createDeployKey" class="secondary">Generar llave SSH</button>@endif</article></div><div x-show="drawer === 'webhook'"><article class="panel" x-data="{ visible: false }"><h2>Webhook de deploy</h2><p class="muted">GitHub y GitLab encolan un deploy cuando haces push a <code>{{ $site->branch }}</code>.</p>@if($site->webhook_token)<label>URL<input readonly value="{{ route('webhooks.deploy', $site) }}"></label>@if($newWebhookSecret)<label>Secret — cópialo ahora<input readonly value="{{ $newWebhookSecret }}"></label>@else<p class="muted">El secret sólo se muestra al crear o rotar el webhook.</p>@endif<div class="button-row"><button wire:click="createWebhook" class="secondary">Rotar webhook</button><button wire:click="disableWebhook" class="ghost">Desactivar</button></div>@else<button wire:click="createWebhook" class="secondary">Crear webhook</button>@endif</article></div><div x-show="drawer === 'health'"><article class="panel" @if($dnsRun && in_array($dnsRun->status, ['queued', 'running'])) wire:poll.1500ms @endif><h2>Salud, DNS y respaldos</h2><div class="stack"><input wire:model="healthUrl" placeholder="https://{{ $site->domain }}"><button wire:click="checkHealth" class="secondary">Verificar salud</button><button wire:click="checkDns" class="secondary">Comprobar propagación DNS</button><button wire:click="backup" class="secondary">Crear backup de archivos</button></div><p class="muted">HTTP: {{ $site->health_status ?? 'sin revisar' }} · Backup: {{ $site->last_backup_at?->diffForHumans() ?? 'nunca' }}</p>@if($site->last_backup_path)<form wire:submit="restoreBackup" class="stack"><label>Para restaurar el último backup escribe <code>RESTAURAR {{ $site->domain }}</code><input wire:model="restoreConfirmation" autocomplete="off"></label><button class="ghost">Restaurar último backup</button></form>@endif @if($dnsRun)<p class="muted">DNS: {{ $dnsRun->status }} @if(in_array($dnsRun->status, ['queued','running']))<button wire:click="cancelDns({{ $dnsRun->id }})" class="ghost">Cancelar</button>@endif</p><div class="dns-results">@foreach($dnsRun->results['checks'] ?? [] as $check)<p><code>{{ $check['resolver'] }}</code> · A: {{ $check['a'] ?: '—' }} · AAAA: {{ $check['aaaa'] ?: '—' }}</p>@endforeach</div>@elseif($site->dns_results)<div class="dns-results">@foreach($site->dns_results['checks'] ?? [] as $check)<p><code>{{ $check['resolver'] }}</code> · A: {{ $check['a'] ?: '—' }} · AAAA: {{ $check['aaaa'] ?: '—' }}</p>@endforeach</div>@endif</article></div><div x-show="drawer === 'logs'"><article class="panel"><h2>Logs</h2><div class="button-row"><button wire:click="logs('laravel')" class="tiny">Laravel</button><button wire:click="logs('nginx-access')" class="tiny">Nginx access</button><button wire:click="logs('nginx-error')" class="tiny">Nginx error</button><button wire:click="logs('queue')" class="tiny">Queue</button></div></article></div><div x-show="drawer === 'domains'"><article class="panel"><h2>Dominios y rollback</h2><div class="stack"><input wire:model="alias" placeholder="www.{{ $site->domain }}"><button wire:click="addAlias" class="secondary">Añadir alias</button></div>@foreach($site->aliases ?? [] as $alias)<p><code>{{ $alias }}</code> <button wire:click="removeAlias('{{ $alias }}')" class="ghost">Quitar</button></p>@endforeach<div class="stack"><input wire:model="rollbackCommit" placeholder="SHA del commit"><button wire:click="rollback" class="ghost">Rollback al commit</button></div></article></div><div x-show="drawer === 'environment'"><section class="panel"><div class="section-title"><div><h2>Archivo .env</h2><p>Se guarda cifrado en la base del panel y se aplica por el agente con permisos 0640.</p></div><button wire:click="saveEnvironment" class="primary">Guardar .env</button></div><textarea wire:model="environment" class="env-editor" rows="18" spellcheck="false" placeholder="APP_NAME=...&#10;APP_ENV=production"></textarea></section></div><div x-show="drawer === 'script'"><section class="panel"><div class="section-title"><div><h2>Script de deploy</h2><p>Un comando por línea. Si lo configuras, sustituye la secuencia Laravel predeterminada y corre antes de levantar el sitio. Acepta <code>php artisan</code>, <code>composer</code> y <code>npm</code>; no ejecuta shell.</p></div><button wire:click="saveDeployCommands" class="primary">Guardar script</button></div><textarea wire:model="deployCommands" class="env-editor" rows="9" spellcheck="false" placeholder="composer install --no-dev --dump-autoload&#10;php artisan migrate --force&#10;npm ci&#10;npm run build&#10;php artisan optimize:clear&#10;php artisan optimize"></textarea>@error('deployCommands')<p class="error">{{ $message }}</p>@enderror</section></div><div x-show="drawer === 'nginx'"><section class="panel"><div class="section-title"><div><h2>Configuración Nginx</h2><p>Directivas dentro del bloque <code>server</code>. Se prueba y revierte automáticamente si Nginx no acepta la configuración.</p></div><button wire:click="saveNginxConfig" class="primary">Aplicar Nginx</button></div><textarea wire:model="nginxConfig" class="env-editor" rows="12" spellcheck="false" placeholder="client_max_body_size 64m;&#10;location /health { return 200; }"></textarea></section></div><div x-show="drawer === 'danger'"><section class="panel danger-zone"><h2>Zona de peligro</h2><p class="muted">Suspender deshabilita Nginx. Eliminar destruye sitio, vhost, servicios, llaves, backups y la base/usuario MariaDB detectados en su <code>.env</code>. Úsalo sólo si esa base no se comparte.</p>@if($site->status === 'suspended')<button wire:click="resumeSite" class="secondary">Reactivar sitio</button>@else<button wire:click="suspendSite" class="ghost">Suspender sitio</button>@endif<form wire:submit="deleteSite" class="stack"><label>Escribe <code>ELIMINAR {{ $site->domain }}</code><input wire:model="dangerConfirmation" autocomplete="off"></label><button class="danger-button">Eliminar sitio definitivamente</button></form></section></div>@if($errors->any())<div class="error" role="alert">@foreach($errors->all() as $error)<p>{{ $error }}</p>@endforeach</div>@endif
</div><footer><button class="secondary" @click="drawer = ''">Volver al sitio</button><span class="muted">Los cambios se aplican con el botón de cada herramienta.</span></footer></section></div></div>
